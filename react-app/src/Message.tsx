// Be sure to use PascalCasing
function Message() {
  // Using JSX to print something simple to the screen
  const name = "Lonnie";
  // Within '{}' we can write any Expression - Any piece of code that produces a value
  return <h1>Hello {name}</h1>;
}
// Exporting it as a default obj
export default Message;
